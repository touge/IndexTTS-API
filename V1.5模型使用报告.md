# IndexTTS-API V1.5 模型使用报告

## 一、概述

本报告分析 IndexTTS-API 项目在 **仅使用 V1.5 版本** 时所需的所有模型。

---

## 二、V1.5 必需模型目录结构

```
models/
│
├── IndexTTS-1.5/                          【必需 - 核心 TTS 模型】
│   ├── config.yaml                        # 模型配置文件
│   ├── gpt.pth                            # GPT 语言模型权重
│   ├── bigvgan_generator.pth              # BigVGAN 声码器生成器权重
│   ├── dvae.pth                           # DVAE 编码解码器权重
│   ├── bpe.model                          # BPE 分词器模型
│   └── unigram_12000.vocab                # Unigram 词表
│
├── facebook/
│   └── w2v-bert-2.0/                      【必需 - 语义特征提取】
│       ├── config.json
│       ├── model.safetensors
│       └── preprocessor_config.json
│
└── whisper/
    └── faster-whisper-large-v2/           【可选 - 字幕生成】
        ├── config.json
        ├── model.bin
        ├── tokenizer.json
        ├── vocabulary.txt
        ├── README.md
        └── .gitattributes
```

---

## 三、V1.5 模型详细清单

### 必用模型（TTS 合成必需）

| 序号 | 模型名称 | 文件/路径 | 用途 | 加载方式 |
|------|----------|-----------|------|----------|
| 1 | **GPT 语言模型** | `models/IndexTTS-1.5/gpt.pth` | 文本→语音特征生成 | 由 indextts 库内部加载 |
| 2 | **BigVGAN 生成器** | `models/IndexTTS-1.5/bigvgan_generator.pth` | 语音特征→波形音频解码 | 由 indextts 库内部加载 |
| 3 | **DVAE** | `models/IndexTTS-1.5/dvae.pth` | 语音特征编码解码器 | 由 indextts 库内部加载 |
| 4 | **BPE 分词器** | `models/IndexTTS-1.5/bpe.model` | 文本 BPE 分词 | 由 indextts 库内部加载 |
| 5 | **Wav2Vec2-Bert 2.0** | `models/facebook/w2v-bert-2.0/` | 参考音频语义特征提取 | `Wav2Vec2BertModel.from_pretrained()` |

### 存在于目录但推理时不加载的文件

| 文件 | 说明 |
|------|------|
| `models/IndexTTS-1.5/bigvgan_discriminator.pth` | BigVGAN 判别器，仅训练时使用，推理不需要 |
| `models/IndexTTS-1.5/unigram_12000.vocab` | Unigram 词表，代码中未引用（可能为 BPE 备选方案） |

### 可选模型

| 序号 | 模型名称 | 路径 | 用途 | 触发条件 |
|------|----------|------|------|----------|
| 1 | **faster-whisper-large-v2** | `models/whisper/faster-whisper-large-v2/` | 音频→文字转录（字幕生成） | `generate_subtitle=true` |

---

## 四、V1.5 模型加载流程

```
API 请求 → QueueManager → _load_model("V1.5")
  ↓
动态导入 app.core.indextts.infer.IndexTTS
  ↓
初始化参数:
  - cfg_path: models/IndexTTS-1.5/config.yaml
  - model_dir: models/IndexTTS-1.5/
  - use_fp16: True
  - device: cuda
  ↓
IndexTTS V1.5 内部依次加载:
  ├── GPT 模型 (gpt.pth)
  ├── BigVGAN 声码器 (bigvgan_generator.pth)
  ├── DVAE (dvae.pth)
  ├── BPE 分词器 (bpe.model)
  └── Wav2Vec2-Bert (facebook/w2v-bert-2.0/)
```

**注意：** V1.5 的 `app.core.indextts.infer.IndexTTS` 是继承自 `indextts.infer.IndexTTS` 的包装类，模型加载逻辑在 indextts 库内部实现。

---

## 五、V1.5 与 V2.0 模型对比

### V1.5 使用的模型

| 模型 | 路径 | 说明 |
|------|------|------|
| GPT | `models/IndexTTS-1.5/gpt.pth` | 单文件权重 |
| BigVGAN | `models/IndexTTS-1.5/bigvgan_generator.pth` | 单文件权重 |
| DVAE | `models/IndexTTS-1.5/dvae.pth` | 单文件权重 |
| Wav2Vec2-Bert | `models/facebook/w2v-bert-2.0/` | HuggingFace 格式 |

### V2.0 使用的模型（V1.5 不需要）

| 模型 | 路径 | 说明 |
|------|------|------|
| IndexTTS-2 | `models/IndexTTS-2/` | V2.0 核心模型 |
| Qwen Emotion | `models/IndexTTS-2/qwen0.6bemo4-merge/` | 情感识别 |
| Semantic Codec | `models/amphion/MaskGCT/semantic_codec/` | 语义编解码 |
| BigVGAN V2 | `models/nvidia/bigvgan_v2_22khz_80band_256x/` | V2 声码器 |
| Mandarin Wav2Vec2 | `models/kehanlu/mandarin-wav2vec2/` | 中文语音特征 |
| CAMPPlus | `models/funasr/campplus/` | 说话人嵌入 |
| Randeng-Pegasus | `models/IDEA-CCNL/Randeng-Pegasus-523M-Summary-Chinese/` | 文本摘要 |
| Chinese-CLIP | `models/ofa-sys/chinese-clip-vit-large-patch14/` | 中文 CLIP |
| Chinese-CLIP 336px | `models/ofa-sys/chinese-clip-vit-large-patch14-336px/` | 中文 CLIP |
| MiniLM | `models/sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2/` | 文本相似度 |

---

## 六、models 目录完整清单

```
models/
├── IndexTTS-1.5/                  ← V1.5 核心模型
├── IndexTTS-2/                    ← V2.0 核心模型（V1.5 不需要）
├── amphion/MaskGCT/               ← V2.0 专用（V1.5 不需要）
├── facebook/w2v-bert-2.0/         ← V1.5 与 V2.0 共用
├── funasr/campplus/               ← V2.0 专用（V1.5 不需要）
├── IDEA-CCNL/                     ← V2.0 专用（V1.5 不需要）
├── kehanlu/                       ← V2.0 专用（V1.5 不需要）
├── MeiGen-AI/                     ← V2.0 专用（V1.5 不需要）
├── nltk_data/                     ← 文本处理工具
├── nvidia/                        ← V2.0 专用（V1.5 不需要）
├── ofa-sys/                       ← V2.0 专用（V1.5 不需要）
├── ollama/                        ← 未知用途
├── qwen/                          ← V2.0 专用（V1.5 不需要）
├── sentence-transformers/         ← V2.0 专用（V1.5 不需要）
├── TencentGameMate/               ← V2.0 专用（V1.5 不需要）
└── whisper/                       ← 字幕生成（可选）
    ├── faster-whisper-large-v2/   ← 配置中指定的模型
    ├── faster-whisper-medium/     ← 额外下载的模型
    ├── base.pt                    ← 原始 whisper 模型
    ├── medium.pt                  ← 原始 whisper 模型
    ├── large.pt                   ← 原始 whisper 模型
    └── ggml-*.bin                 ← GGML 格式模型
```

---

## 七、总结

### 只运行 V1.5 必需的模型

| 模型目录 | 必要性 | 说明 |
|----------|--------|------|
| `models/IndexTTS-1.5/` | **必需** | 核心 TTS 模型（GPT + BigVGAN + DVAE + Tokenizer） |
| `models/facebook/w2v-bert-2.0/` | **必需** | 语义特征提取 |
| `models/whisper/faster-whisper-large-v2/` | **可选** | 仅字幕生成时需要 |

### 只运行 V1.5 可以删除的目录

以下目录在 **仅使用 V1.5** 时可以安全删除：

- `models/IndexTTS-2/`
- `models/amphion/`
- `models/funasr/`
- `models/IDEA-CCNL/`
- `models/kehanlu/`
- `models/MeiGen-AI/`
- `models/nvidia/`
- `models/ofa-sys/`
- `models/ollama/`
- `models/qwen/`
- `models/sentence-transformers/`
- `models/TencentGameMate/`
- `models/whisper/base.pt`、`medium.pt`、`large.pt`、`ggml-*.bin`（如不需要原始 whisper）
- `models/whisper/faster-whisper-medium/`（如不需要 medium 版本）

---

*报告生成时间：2026-05-23*